name: Build & Deploy Admin Panel (Helm)

on:
  push:
    branches:
      - main
      - staging
    paths-ignore:
      - '.github/**'
      - '**.md'
      - '.argo/**'
  pull_request:
    branches:
      - main
      - staging
    paths-ignore:
      - '.github/**'
      - '**.md'
      - '.argo/**'

permissions:
  contents: read
  actions: read

env:
  REGISTRY: harbor.goalixa.com

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set image repository and deployment config
        id: config
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            BRANCH="${{ github.base_ref }}"
          else
            BRANCH="${{ github.ref_name }}"
          fi

          if [[ "$BRANCH" == "staging" ]]; then
            echo "repository=admin_panel-staging/admin" >> $GITHUB_OUTPUT
            echo "image_tag=latest" >> $GITHUB_OUTPUT
            echo "environment=staging" >> $GITHUB_OUTPUT
            echo "app_name=goalixa-admin-staging" >> $GITHUB_OUTPUT
          elif [[ "$BRANCH" == "main" ]]; then
            echo "repository=admin_panel/admin" >> $GITHUB_OUTPUT
            echo "image_tag=latest" >> $GITHUB_OUTPUT
            echo "environment=production" >> $GITHUB_OUTPUT
            echo "app_name=goalixa-admin" >> $GITHUB_OUTPUT
          else
            echo "Unsupported branch: $BRANCH"
            exit 1
          fi

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Harbor
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ secrets.HARBOR_USERNAME }}
          password: ${{ secrets.HARBOR_PASSWORD }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ steps.config.outputs.repository }}
          tags: |
            type=raw,value=latest

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          push: true
          platforms: linux/amd64,linux/arm64
          tags: ${{ steps.meta.outputs.tags }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          labels: |
            org.opencontainers.image.source=${{ github.repositoryUrl }}
            org.opencontainers.image.revision=${{ github.sha }}

      - name: Setup kubectl
        run: |
          if [ -n "${{ secrets.KUBE_CONFIG }}" ]; then
            echo "${{ secrets.KUBE_CONFIG }}" | base64 -d > /tmp/kubeconfig
            echo "KUBECONFIG=/tmp/kubeconfig" >> $GITHUB_ENV
            echo "HAS_KUBE_CONFIG=true" >> $GITHUB_ENV
          else
            echo "HAS_KUBE_CONFIG=false" >> $GITHUB_ENV
          fi

      - name: Update ArgoCD application
        if: env.HAS_KUBE_CONFIG == 'true'
        run: |
          IMAGE_TAG="${{ steps.config.outputs.image_tag }}"
          APP_NAME="${{ steps.config.outputs.app_name }}"
          ENVIRONMENT="${{ steps.config.outputs.environment }}"

          echo "Updating ArgoCD application '${APP_NAME}' (${ENVIRONMENT}) with image tag: ${IMAGE_TAG}"

          kubectl patch application ${APP_NAME} \
            -n argocd \
            --type='merge' \
            -p="{
              \"spec\": {
                \"source\": {
                  \"helm\": {
                    \"parameters\": [
                      {
                        \"name\": \"image.tag\",
                        \"value\": \"${IMAGE_TAG}\"
                      }
                    ]
                  }
                }
              }
            }"

          kubectl annotate application ${APP_NAME} \
            -n argocd \
            "force-sync-at=$(date +%s)" \
            --overwrite

          echo "ArgoCD application '${APP_NAME}' updated for ${ENVIRONMENT}"

      - name: Summary
        run: |
          ENVIRONMENT="${{ steps.config.outputs.environment }}"
          IMAGE_TAG="${{ steps.config.outputs.image_tag }}"
          REPOSITORY="${{ steps.config.outputs.repository }}"
          APP_NAME="${{ steps.config.outputs.app_name }}"
          FULL_IMAGE="${{ env.REGISTRY }}/${REPOSITORY}:${IMAGE_TAG}"
          echo "## Build and Deploy Complete (${ENVIRONMENT})" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Environment:** ${ENVIRONMENT}" >> $GITHUB_STEP_SUMMARY
          echo "- **Image Tag:** ${IMAGE_TAG}" >> $GITHUB_STEP_SUMMARY
          echo "- **Full Image:** ${FULL_IMAGE}" >> $GITHUB_STEP_SUMMARY
          echo "- **Registry:** ${FULL_IMAGE}" >> $GITHUB_STEP_SUMMARY
          echo "- **ArgoCD App:** ${APP_NAME}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY

          if [ "${{ env.HAS_KUBE_CONFIG }}" = "true" ]; then
            echo "- **Deployment Method:** Helm (ArgoCD - Auto-sync)" >> $GITHUB_STEP_SUMMARY
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "### ArgoCD will now:" >> $GITHUB_STEP_SUMMARY
            echo "1. Detect Helm parameter change for ${APP_NAME}" >> $GITHUB_STEP_SUMMARY
            echo "2. Render Helm chart with image tag: ${IMAGE_TAG}" >> $GITHUB_STEP_SUMMARY
            echo "3. Deploy to Kubernetes namespace: ${ENVIRONMENT}" >> $GITHUB_STEP_SUMMARY
          else
            echo "- **Deployment Method:** Helm (Manual sync required)" >> $GITHUB_STEP_SUMMARY
          fi
